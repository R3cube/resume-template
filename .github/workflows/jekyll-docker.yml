name: Jekyll site CI + PDF

on:
  push:
    branches: [ "employee/**" ]
  pull_request:
    branches: [ "employee/**" ]


jobs:
  wait-for-pages:
    runs-on: ubuntu-latest
    outputs:
      ready: ${{ steps.check.outputs.ready }}
    steps:
      - name: Wait for Pages to finish
        run: |
          echo "Waiting 120 seconds for GitHub Pages deployment to complete..."
          sleep 120

  generate-pdf:
    runs-on: ubuntu-latest
    needs: wait-for-pages
    if: needs.wait-for-pages.outputs.ready == 'true'
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Read site name from _config.yml
        id: meta
        run: |
          SITE_NAME=$(grep '^name:' _config.yml | sed 's/name:[ ]*//')
          FILE_NAME="${SITE_NAME}.pdf"
          echo "file_name=${FILE_NAME}" >> $GITHUB_OUTPUT

      - name: Install wkhtmltopdf
        run: |
          sudo apt-get update
          sudo apt-get install -y wkhtmltopdf

      - name: Generate PDF from GitHub Pages
        run: |
          RANDOM_QUERY=$(date +%s)
          SITE_URL="https://<USERNAME>.github.io/<REPO>/?cache=${RANDOM_QUERY}"
          echo "Fetching from: $SITE_URL"
          wkhtmltopdf \
            --orientation Portrait --page-size A4 \
            --margin-bottom 0 --margin-left 0 --margin-right 0 --margin-top 0 \
            --javascript-delay 2000 --enable-local-file-access \
            "$SITE_URL" "${{ steps.meta.outputs.file_name }}"

      - name: Upload PDF
        uses: actions/upload-artifact@v4
        with:
          name: ${{ steps.meta.outputs.file_name }}
          path: ${{ steps.meta.outputs.file_name }}

      # ---------- Google Drive Upload ----------
      # - name: Install rclone
      #   if: ${{ secrets.GDRIVE_SERVICE_ACCOUNT != '' }}
      #   run: curl https://rclone.org/install.sh | sudo bash
      #
      # - name: Configure rclone for Google Drive
      #   if: ${{ secrets.GDRIVE_SERVICE_ACCOUNT != '' }}
      #   run: |
      #     echo "${{ secrets.GDRIVE_SERVICE_ACCOUNT }}" > sa.json
      #     rclone config create gdrive drive scope=drive.file service_account_file=sa.json
      #
      # - name: Upload to Google Drive
      #   if: ${{ secrets.GDRIVE_SERVICE_ACCOUNT != '' }}
      #   run: |
      #     # Replace YOUR_FOLDER_ID with the Drive folder ID where PDFs should go
      #     rclone copy "${{ steps.meta.outputs.file_path }}" gdrive:"{YOUR_FOLDER_ID}" --drive-root-folder-id {YOUR_FOLDER_ID}
